<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/public/stylesheets/style.css" rel="stylesheet">
</head>
<body>

<div class="container my-5">

  <div class="proyecto-header">
    <h2>📷 Proyecto de Galería de Fotos</h2>
    <p>Aplicación creada con Express + Sequelize + EJS</p>
  </div>

  <h1 class="text-center mb-4"><%= title %></h1>

  <table class="table table-hover table-bordered align-middle text-center">
    <thead>
      <tr>
        <th>ID</th>
        <th>Título</th>
        <th>Descripción</th>
        <th>Calificación</th>
        <th>Imagen</th>
      </tr>
    </thead>
    <tbody>
      <% arrFotos.forEach(foto => { %>
        <tr>
          <td><%= foto.id %></td>
          <td><%= foto.titulo %></td>
          <td><%= foto.descripcion %></td>
          <td><%= foto.calificacion %></td>
          <td>
            <% if (foto.ruta) { %>
              <img src="/public/<%= foto.ruta %>" alt="Foto" class="img-thumbnail" style="max-width: 100px;">
              <br><small>Ruta: <%= foto.ruta %></small>
            <% } else { %>
              <span class="text-muted">Sin imagen</span>
            <% } %>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>